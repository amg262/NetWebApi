{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "AllowedHosts": "*",
  "MongoDbSettings": {
    "Host": "localhost",
    "Port": "27017",
    "User":"mongoadmin"
    // "ConnectionString": "mongodb://localhost:27017" is what will be used if you don't specify the host and port
    // The settings will be accessed by the Program.cs file, using the MongoDbSettings class and ConnectionString property
    // 
  }
}
